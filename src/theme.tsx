"use client"
// theme.js
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#009dff',
    },
    secondary: {
      main: '#e8ffff',
    },
  },
});

export default theme;